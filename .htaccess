# ============================================================
# .htaccess — Configuration Apache (Laragon)
# ============================================================

# Sécurité : empêcher la liste des fichiers du dossier
Options -Indexes

# Page d'accueil par défaut
DirectoryIndex index.php index.html

# COMPRESSION : réduit la taille des fichiers envoyés (performance mobile)
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json
</IfModule>

# CACHE : les fichiers statiques sont gardés en cache navigateur
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css            "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
    ExpiresByType image/png           "access plus 1 month"
    ExpiresByType image/svg+xml       "access plus 1 month"
</IfModule>

# Protection du dossier api/ (refuser l'accès direct au navigateur)
<FilesMatch "config\.php$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
